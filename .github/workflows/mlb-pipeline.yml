name: MLB Pipeline

on:
  schedule:
    # 1) Early morning picks — 7 AM ET (11:00 UTC)
    #    Get picks up for morning visitors. Probable pitchers known, lineups may be projected.
    - cron: '0 11 * * *'

    # 2) Atlas refresh + re-run — 9 AM ET (13:00 UTC)
    #    Pull latest Statcast data, regenerate with any new lineup info
    - cron: '0 13 * * *'

    # 3) Lineup refresh — 2 PM ET (18:00 UTC)
    #    Early lineups start dropping
    - cron: '0 18 * * *'

    # 4) Main refresh — 5 PM ET (21:00 UTC)
    #    Most lineups confirmed by now
    - cron: '0 21 * * *'

    # 5) Late refresh — 6:30 PM ET (22:30 UTC)
    #    Final lineup check before 7 PM first pitch
    - cron: '30 22 * * *'

    # 6) Settle picks — 2 AM ET (06:00 UTC)
    #    All games done, mark results overnight
    - cron: '0 6 * * *'

  # Auto-overwrite when old bot pushes broken code to mlbsim/index.html
  push:
    paths:
      - 'mlbsim/index.html'

  workflow_dispatch:
    inputs:
      run_type:
        description: 'What to run'
        required: true
        default: 'picks'
        type: choice
        options:
          - picks
          - atlas-refresh
          - both

jobs:
  atlas-refresh:
    runs-on: ubuntu-latest
    if: >-
      github.event_name == 'workflow_dispatch' &&
      (inputs.run_type == 'atlas-refresh' || inputs.run_type == 'both') ||
      github.event.schedule == '0 13 * * *'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install pybaseball numpy pandas requests

      - name: Refresh 2026 atlas
        run: python3 scripts/refresh_atlas_2026.py

      - name: Commit atlas updates
        run: |
          git config user.name "MLB Pipeline Bot"
          git config user.email "noreply@github.com"
          git add atlas/
          if git diff --cached --quiet; then
            echo "No atlas changes to commit"
          else
            git commit -m "Atlas refresh — $(TZ=America/New_York date +'%b %d %I:%M %p ET')"
            git push
          fi

  generate-picks:
    runs-on: ubuntu-latest
    needs: [atlas-refresh]
    if: >-
      always() &&
      (needs.atlas-refresh.result == 'success' || needs.atlas-refresh.result == 'skipped') &&
      (
        (github.event_name == 'push' && github.actor != 'github-actions[bot]') ||
        github.event_name == 'workflow_dispatch' &&
        (inputs.run_type == 'picks' || inputs.run_type == 'both') ||
        github.event.schedule == '0 11 * * *' ||
        github.event.schedule == '0 13 * * *' ||
        github.event.schedule == '0 18 * * *' ||
        github.event.schedule == '0 21 * * *' ||
        github.event.schedule == '30 22 * * *'
      )
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install requests

      - name: Pull latest (in case atlas job pushed)
        run: git pull origin main

      - name: Generate MLB picks
        run: python3 scripts/build_mlb_sim.py

      - name: Commit and push
        run: |
          git config user.name "MLB Pipeline Bot"
          git config user.email "noreply@github.com"
          git add mlbsim/index.html
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Auto-sync MLB SIM — $(TZ=America/New_York date +'%b %d %I:%M %p ET')"
            git push
          fi

  settle-picks:
    runs-on: ubuntu-latest
    if: github.event.schedule == '0 6 * * *'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install requests

      - name: Generate settled picks
        run: python3 scripts/build_mlb_sim.py

      - name: Commit and push
        run: |
          git config user.name "MLB Pipeline Bot"
          git config user.email "noreply@github.com"
          git add mlbsim/index.html index.html
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Settle MLB picks — $(TZ=America/New_York date +'%b %d %I:%M %p ET')"
            git push
          fi
